#include<stdio.h>
#include<stdlib.h>

int decimal(int binario){
    
    

    return 0;
}

int main(int argc, char *argv[])
{

    char *binario = argv[1];
    printf("EL numero  binario  ingresado es: %s\n", binario);
    int entero = atoi(binario);
    int dec = decimal(entero);
    return 0;
}