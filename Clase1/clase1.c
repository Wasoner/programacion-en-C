#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <conio.h>


int main(){
    
   printf("Ingrese un numero: ");
   int num;
   scanf("%d", &num);
    for (int i = 1; i < num; i++)
    {
        int n = num%i;
        printf("%d \n",n);
        for (int i = 2; i < num; i++)
        {
            if (num%)
            {
                /* code */
            }
            
        }
        
        
    }
    return 0;
}