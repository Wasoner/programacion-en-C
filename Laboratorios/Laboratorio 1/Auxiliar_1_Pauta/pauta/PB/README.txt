como veran en este ejercicios tenemos varios archivos,
¿como compilamos varios archivos en un solo ejecutable?
1. deben crear un modulo objeto (archivo .o) por cada uno de los .c
2. deben enlazarlos en un solo ejecutable (ejemplo de los colores en el enunciado)

IMPORTANTE: cuando enlace los .o al ejecutable, debe poner la flag -lm al final del comando, dado que estamos utilizando la libreria math.h

(referirse al enunciado para los comandos)
