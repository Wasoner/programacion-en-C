// los archivos .h incluyen unicamente declaraciones de funciones
// de esta forma, los archivos que incluyan este .h saben que en algun lugar
// existira el cuerpo de esta funcion, por lo que los archivos compilaran
// exitosamente (pero recuerden que el cuerpo efectivamente debe existir)

int decimal(int binario);
